package com.example.toygry.studykotlin.ch01

class Lec01Kotlin {

    fun main() {

        // 초기화 하지 않으면 error
        var number1 : Long = 10L; // 가변
        val number2 : Long = 10L; // 불변

        // null 이 들어갈 수 있는 경우 ?를 붙임
        var number3 :Long? = null;

        // kotlin 에서는 객체 인스턴스 화 시 new 를 붙이지 않는동
    }
}